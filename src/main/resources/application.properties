spring.application.name=Metroservice
server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/metro_db
spring.datasource.username=postgres
spring.datasource.password=Coder@083
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Define the SOS Alert Queue
rabbitmq.queue.sos-alert=sos-alert-queue

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379

# Logging Configuration
#logging.level.org.springframework=INFO
#logging.level.com.namma.metro=DEBUG
